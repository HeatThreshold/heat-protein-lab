# Session — 2026-05-25 — Phase 0 smoke tests via agy CLI

> Third session log of the day. Earlier logs: [stitch-ui](./2026-05-25-stitch-ui.md) covers Antigravity-desktop's parallel Stitch mockup generation, [devrel-and-security](./2026-05-25-devrel-and-security.md) covers the framework setup and the API-key incident. This log covers driving the `agy` CLI (Antigravity's terminal mode) through all six Phase 0 Science Skill smoke tests.

## Meta

- **Date:** 2026-05-25
- **Duration:** ~25 minutes wall-clock; ~6 minutes of actual skill execution after the first attempt stalled
- **Phase / chapter:** Phase 0 — final smoke tests + `data/candidates.json`
- **Where it left off:** All six smoke tests passing, `data/candidates.json` written, three sharp edges in the science plugin surfaced (worth lifting into the Beat 1 post).

## What was attempted

Use `agy --print --dangerously-skip-permissions` (the CLI version of Antigravity, distinct from the desktop IDE used in the first session) to run the six Phase 0 smoke tests from PROJECT.md against the live science databases, write the verified IDs to `data/candidates.json`, and create LICENSE_NOTIFICATION.txt files in each skill directory.

## How it was driven

Initial attempt: one mega-prompt asking agy to do all six skills sequentially and synthesize the JSON. **It stalled.** PID 3230380 ran 14+ minutes at 0.0% CPU, never spawned a single `uv` subprocess, never wrote anything to disk. Process was sleeping on one network socket — appeared to be in an LLM call that never returned a tool invocation. Killed via `pkill`.

Pivot: one focused prompt per skill, run in parallel as five sibling bash calls (after a single-skill PDB test confirmed the pattern worked). Each prompt was 3–5 sentences, included the exact `uv run` command to execute, and capped the response at "under 5 lines." Total wall-clock for the five parallel skills: about 4 minutes. The PDB pilot took roughly 30 seconds.

**Why this matters for Beat 1:** there's a real lesson in there — when prompting an agentic IDE, smaller and more direct prompts converge much faster than orchestration prompts. The instinct to "let it plan the whole thing" cost 15 minutes of nothing; "do one thing, here's the exact command" produced verified output in 30 seconds.

## Stitch prompts used

None this session.

## Science Skill calls

| Skill | Intent | First-try success? | Output |
|-------|--------|--------------------|--------|
| `pdb_database` | metadata for `4PO2` | yes | `/tmp/` — title verified as HSP70 SBD-in-complex |
| `alphafold_database_fetch_and_analyze` | fetch + analyze `P0DMV8` (HSPA1A) | **no, then yes** — 403 on first request, agy set `SCIENCE_SKILLS_USER_AGENT` env var and re-ran | `/tmp/af-hspa1a/` → copied to `data/structures/alphafold/hspa1a/` |
| `human_protein_atlas_database` | `HSPA1A` → Ensembl + tissue expression | **no, then yes after script patch** — `hpa_cli.py` had a `ValueError` in the tissue-expression call; agy patched it in place at the skill directory | `/tmp/hpa-hspa1a-{id,expression}.json` |
| `pubmed_database` | search "HSF1 trimerization", top 3 abstracts | yes | `/tmp/pubmed-hsf1-search.json`, `pubmed-hsf1-abstracts.json` |
| `clinvar_database` | count variants in `HSPA1A[gene]` | yes | `/tmp/clinvar-hspa1a-count.json` — 47 variants |
| `reactome_database` | search "Cellular response to heat stress" | yes | `/tmp/reactome-hsr-search.json` — R-HSA-3371556 verified |

**Sharp edges worth reporting upstream to DeepMind:**
- AlphaFold endpoint returning 403 without a User-Agent header was non-obvious; future operators picking the science plugin up clean will hit this. The skill could ship a default UA.
- The HPA `hpa_cli.py` ValueError on `get-tissue-expression` is a real bug; agy patched it locally but the upstream script still has it. Worth a GitHub issue against `google-deepmind/science-skills`.

## Modern Web Guidance queries

None this session.

## Time-to-first-useful-output

- First attempt (mega-prompt): never produced output before being killed at ~14 min.
- Pivoted approach: first verified result (PDB title) in **~30 seconds** from prompt issued to clean stdout.

## Surprises

- **The "do everything" prompt failed, the "do one specific thing" prompts all succeeded.** This is probably the most important paragraph the Beat 1 post can publish about agentic IDE prompting — be specific, be small, run in parallel where independent.
- **agy is willing to patch upstream plugin scripts to make things work.** It edited `~/.gemini/config/plugins/science/skills/human_protein_atlas_database/scripts/hpa_cli.py` in place to fix the ValueError. That's both impressive (it didn't give up) and worth flagging (the operator now has a divergent local copy of an upstream skill script, which will conflict with any plugin update).
- **All six skills returned verifiable, citable, useful biology.** The page can now anchor real chapters in real data without anyone hand-typing accession numbers.

## Frustrations / sharp edges

- The 14-minute stall on the mega-prompt was opaque from outside — `ps` showed 0.0% CPU, no spawned subprocesses, nothing in stdout, just one open socket. Without `pkill` access this would have been hard to debug.
- agy copied a 31 KB version of `reactome_analysis.py` into `scripts/` with hardcoded local paths and modified imports. Not portable — anyone cloning the repo on a different machine would have broken paths. Deleted from this commit; future Reactome work should be a small project-specific wrapper, not a vendored copy of the skill script.
- The 951 KB AlphaFold PAE matrix is heavy for git; kept it in `/tmp/` rather than committing. Future fetcher scripts should leave PAE in a non-committed location and only commit metadata + .cif.

## Screenshots

- N/A from CLI mode. Worth grabbing once a chapter renders.

## Beat it feeds

- **Beat 1 (the framework post)** — primary beneficiary. This session is the entire Phase 0 trigger.
- The "be specific, be small, run in parallel" lesson is publishable on its own, possibly worth a standalone short post.

## Next session

1. Open Chapter 1 (HSF1) work — verify the HSF1 UniProt accession (probably Q00613) and the HSF1 DBD PDB ID (5D5W or 5D5U candidates from DESIGN.md) via the same per-skill `agy --print` pattern.
2. Re-attempt Ch 4 Stitch mockup HTML (the missing one from the desktop IDE session).
3. Land the Beat 1 post draft.

## Phase 1 kickoff addendum (same calendar day, immediately after)

Phase 1 (Chapter 1 / HSF1) data layer completed in the same calendar day. Pattern: same per-skill `agy --print` calls in parallel for verification, then a project script (`scripts/01_hsf1.py`) called the same skill CLIs by absolute path to aggregate outputs.

- **Real DESIGN.md correction caught.** Agy verified that PDB **5D5W is a fungal homolog** (Chaetomium thermophilum Skn7 with HSE DNA), not human HSF1. DESIGN.md previously listed "5D5W or 5D5U" as Chapter 1 candidates. Chapter 1 now anchors on **5D5U** (human Hsf1 with HSE DNA, X-ray 2.91 Å). This is the first scientific-accuracy fix the Phase 0/1 verification flow produced and is a publishable Beat 1 paragraph in its own right.
- **AlphaFold HSF1 (Q00613)**: global pLDDT **61.31**, two ordered domains (residues 16–84 = DBD, 127–200 = trimerization), 46.7% disordered. Confirms the chapter's "flexible, sensor-like" framing.
- **HPA HSF1 (ENSG00000185122)** — stalled in agy CLI (same 0% CPU / no subprocess pattern as the earlier mega-prompt), succeeded immediately when invoked directly by `scripts/01_hsf1.py` from a plain Python `subprocess.run`. So the stall is specific to `agy --print` orchestration in the CLI, not to the underlying skill. **The same skill that hangs under agy completes in seconds when called directly.** That's worth a paragraph on its own.
- All Chapter 1 data is now in tree:
  - `data/structures/pdb/hsf1/pdb_00005d5u.cif.gz` (74 KB)
  - `data/structures/alphafold/hsf1/AF-Q00613-F1-{metadata.json,model_v6.cif}` (460 KB)
  - `data/citations/hsf1.json` (top 3 PubMed: 30467350, 33493517, 27354066)
  - `data/expression/hsf1.json` (HSF1 across 30+ HPA tissues, high in cerebellum/heart/cervix/breast, broadly expressed)

Phase 1 is now blocked only on **Chapter 1 page scaffold** (HTML/CSS/JS + 3Dmol viewer + scrollytelling shell) — i.e., the rendering side. That's the next session.
